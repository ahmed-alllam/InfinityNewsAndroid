<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.infinitynews.infinitynews">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        android:windowSoftInputMode="adjustResize"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".ui.activities.PostDetailActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".ui.activities.SourcesActivity"
            android:label="@string/news_sources"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ui.activities.EditProfileActivity"
            android:label="@string/edit_profile"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ui.activities.SettingsActivity"
            android:label="@string/settings"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ui.activities.SearchActivity"
            android:label="@string/search"
            android:theme="@style/AppTheme" />
        <activity android:name=".ui.activities.ChooseCategoriesActivity" />
        <activity
            android:name=".ui.activities.LoginSignupActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".ui.activities.TutorialActivity" />
        <activity android:name=".ui.activities.MainActivity" />
        <activity android:name=".ui.activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>